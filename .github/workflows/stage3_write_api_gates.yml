name: stage3_write_api_gates

on:
  pull_request:
    branches: ["**"]
  push:
    branches: ["main", "master"]

jobs:
  stage3_write_api_gates:
    name: stage3_write_api_gates
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Stage 3 v1 write gates
        run: ./scripts/stage3_write_api_gates.sh
